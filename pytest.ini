[pytest]
testpaths = tests
addopts = -ra
markers =
    extractor: tests for extraction/decode pipeline and container tooling
    graphics: graphics and atlas decoding tests
    decode: text/binary decode tests
    smoke: fast smoke tests for contract/reference regressions (including graphics frames.json contract smoke)
